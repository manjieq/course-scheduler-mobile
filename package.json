{
  "name": "course-scheduler-mobile",
  "private": true,
  "scripts": {
    "dev:mobile": "turbo run dev --filter=mobile",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.6.0"
  },
  "packageManager": "pnpm@11.22.0",
  "engines": {
    "node": ">=22.13"
  }
}
