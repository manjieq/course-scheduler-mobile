# extract-course-chat (Phase 4)

Same pipeline as `extract-course-scan` — server-side university
resolution, `_shared` provider interface, schema validation, returns
unsaved structured data for the confirm/edit review screen — but takes
free-text input describing a class instead of a photo.
