# Shared Edge Function code

Phase 4 target: the swappable vision-LLM provider interface (the user
supplies their own API key via Supabase project secrets — never hardcode a
provider or bundle a key client-side) and the request/response schema
validators used by both `extract-course-scan` and `extract-course-chat`.

Not built yet — this directory is a placeholder from the Phase 1 scaffold.
